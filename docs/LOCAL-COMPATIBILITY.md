# Local compatibility gate

Before downloading, cloning, or setting up an experiment repository, verify
that its required runtime and hardware are available on the current machine.

On this Mac, do not download CUDA-only, unavailable-GPU, or otherwise
non-runnable targets for immediate experiments. If a repository is interesting
but cannot run locally, record it as a future remote-GPU target without cloning
it.
